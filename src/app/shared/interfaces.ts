import { colorEnum } from "./list-workers/enums";

export interface IColor {
        value: colorEnum;
        viewValue: string;
      }


      export enum roleUserOrganizationEnum {
        superUser = 'superUser',
        admin = 'admin',
        user = 'user',
      }
      