export enum colorEnum {
        red = 'red',
        orange = 'orange',
        green = 'green'
}

export enum roleUserTeamEnum {
        admin = 'admin',
        user = 'user',
}

export enum phaseEnum {
        expired = 'expired',
        left10 = 'left10',
}

